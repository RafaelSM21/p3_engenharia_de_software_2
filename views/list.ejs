<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Tickets</title>
</head>
<body>
    <h1>Lista de Tickets</h1>
    <ul>
        <% tickets.forEach(ticket => { %>
            <li><%= ticket.natureza %> - <%= ticket.descricao %> - <%= ticket.provedor %></li>
        <% }); %>
    </ul>
</body>
</html>
